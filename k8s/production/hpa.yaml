apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: doc-executor
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1beta1
    kind: Deployment
    name: doc-executor
  minReplicas: 2
  maxReplicas: 2
